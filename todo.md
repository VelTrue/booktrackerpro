## Список задач

- [x] Список жанров: фиксированный список из 14 жанров (`src/constants/genres.ts`) — Роман, Повесть, Рассказ, Детектив, Фэнтези, Научная фантастика, Любовный роман, Триллер, Исторический роман, Поэзия, Сатира, Приключения, Автофикшен, Нон-фикшн.
- [x] Модель данных: добавлено `genres?: Genre[]` (0–2 значения) в `Book` (`src/types.ts`), без миграции — старые книги считаются без жанра.
- [x] Сервис поиска метаданных `src/services/googleBooksApi.ts`: поиск по названию/автору через Google Books API, свободный текстовый запрос (надёжнее находит переводные издания, чем строгий intitle:/inauthor:), до 3 кандидатов (обложка HTTPS, категории), понятные ошибки на русском, 0 результатов — не ошибка.
  - [x] Поддержка бесплатного API-ключа через `VITE_GOOGLE_BOOKS_API_KEY` (см. `.env.example`) — без ключа анонимные запросы почти сразу упираются в 429; пробовали Open Library как бесплатную альтернативу без ключа, но у неё оказалось намного хуже покрытие русских переводных изданий (проверено напрямую), поэтому вернулись на Google Books + ключ.
- [x] Сопоставление категорий с жанрами `src/utils/genreMapping.ts`: эвристика по ключевым словам (BISAC-категории Google Books → наши 14 жанров), возвращает до 2 жанров.
- [x] Утилиты подсчёта жанров `src/utils/genreUtils.ts`: подсчёт книг по каждому из 14 жанров для радар-диаграммы.
- [x] Форма добавления/редактирования книги (`src/components/BookForm.tsx`):
  - [x] Кнопка "Найти в интернете" рядом с названием/автором (активна при заполненном названии).
  - [x] Показ до 3 карточек-кандидатов (обложка, название, автор) для выбора.
  - [x] Выбор кандидата подставляет обложку и до 2 жанров; локальная загрузка+кроппер обложки остаётся как есть.
  - [x] Два select'а "Жанр 1"/"Жанр 2" (опционально, фиксированный список), редактируются вручную.
- [x] Радар-диаграмма жанров `src/components/GenreRadarChart.tsx`: многоугольник из 14 вершин (по одной на жанр), длина луча — число книг жанра относительно максимума; пустое состояние — точка в центре.
- [x] Радар-диаграмма встроена в `PieChart.tsx` — второй круг справа от текущей кольцевой диаграммы статусов, внутри той же карточки "Соотношение статусов" (не отдельная карточка и не в "Посмотреть добавленные").
- [x] Фильтр по жанру в "Посмотреть добавленные" (`src/components/BookBrowser.tsx`) — select из 14 жанров + "Без жанра", рядом с фильтром по статусу.
- [x] Сборка: `tsc -b && vite build` проходит без ошибок.
- [ ] Визуальная проверка в браузере (поиск в интернете, выбор кандидата, добавление/редактирование книг с разными жанрами, вид радар-диаграммы с 0 и несколькими жанрами) — не удалось выполнить в этой рабочей среде (сеть до localhost заблокирована), нужно проверить руками через `npm run dev`.
